<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="PlannerDAO">

	<insert id="insertPlanner" >
		INSERT INTO PLANNER (
		USER_ID , ROOM_NAME , PLANNER_START , PLANNER_END , PLANNER_AREA , PLANNER_TITLE , PLANNER_DAY
		) VALUES (
		#{user_id} , #{room_name} , #{planner_start} ,#{planner_end} 
		,#{planner_area} ,#{planner_title} ,#{planner_day}
		)
	</insert>

</mapper>
